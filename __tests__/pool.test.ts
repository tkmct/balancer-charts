import { Pool } from '../model/pool'

describe('Pool', () => {
  test('pool includes token', () => {
    const pool = new Pool()
  })

  test('pool calculate liquidity of a pair', () => {
    const pool = new Pool()
  })
})
